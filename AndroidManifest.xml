<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.senai.saudacao"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="15" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".PrincipalActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".SaudacaoActivity"
            android:label="@string/title_activity_saudacao" >
        </activity>
        <activity
            android:name=".SaudacaoAulaActivity"
            android:label="@string/title_activity_saudacao_aula" >
        </activity>
        <activity
            android:name=".PrincipalAulaActivity"
            android:label="@string/title_activity_principal_aula" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
